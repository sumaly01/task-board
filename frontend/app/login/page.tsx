// Day 5: login form, API call to POST /auth/login, JWT stored in httpOnly cookie
export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p>Login page — coming Day 5</p>
    </main>
  );
}
